# AOI Defect History - データフロー解説

## 目次

- [概要](#概要)
- [現在のデータフロー](#現在のデータフロー)
- [データ保存先の構成](#データ保存先の構成)
- [データ同期とマージ処理](#データ同期とマージ処理)
- [懸念点](#懸念点)
- [改善点](#改善点)
- [最適化された構成の提案](#最適化された構成の提案)

---

## 概要

AOI Defect Historyアプリケーションは、複数のデータ保存先にデータを保存・同期する分散型のデータ管理システムです。

**主なデータ保存先:**
1. **ローカルSQLiteデータベース** (`aoi_data.db`)
2. **共有SQLiteデータベース** (共有ネットワークディレクトリ)
3. **Kintone API** (クラウドサービス)
4. **CSVファイル** (データディレクトリ)
5. **画像ファイル** (データディレクトリ)

---

## 現在のデータフロー

### 1. データ入力フロー

```
ユーザー入力
  ├─ シリアルNo
  ├─ 不良情報（リファレンス、不良項目）
  ├─ 座標情報（画像上でのクリック位置）
  └─ ユーザー情報（AOI担当者）
         │
         ▼
   メモリ上（defect_list）
         │
         ├─→ 画像ファイル生成・保存
         │      (data_directory/{lot_number}/{lot_number}_{board_index}_{defect_number}.png)
         │
         ├─→ ローカルSQLiteデータベース
         │      (非同期: __insert_defect_info_to_db_async)
         │
         ├─→ Kintone API
         │      (非同期: post_kintone_record_async)
         │
         └─→ CSVファイル（変更時）
               (非同期: defect_list_to_csv_async)
```

### 2. データ読み込みフロー

```
アプリケーション起動
         │
         ├─→ 設定ファイル読み込み (settings.ini)
         │      ├─ image_directory (画像ディレクトリ)
         │      ├─ data_directory (データディレクトリ)
         │      ├─ schedule_directory (スケジュールディレクトリ)
         │      └─ shared_directory (共有データベースディレクトリ)
         │
         ├─→ 共有SQLiteデータベース確認
         │      ├─ 存在する場合: ローカルにコピー
         │      └─ 存在しない場合: 新規作成
         │
         ├─→ ローカルSQLiteデータベース作成
         │
         └─→ SMTスケジュール読み込み（非同期）
               (schedule_directory から pandas DataFrame として読み込み)

指図変更時 (change_lot)
         │
         ├─→ SMTスケジュールから指図情報検索
         │      └─ 機種コード、ライン名の取得
         │
         ├─→ 画像ファイル検索・読み込み
         │      (image_directory から品目コードで検索)
         │
         └─→ 不良データ読み込み
               ├─ ローカルSQLiteデータベースから読み込み (優先)
               └─ CSVファイルから読み込み (フォールバック)
```

### 3. データ同期フロー

```
指図変更時 (change_lot)
         │
         ├─→ 現在の不良データをKintone APIに送信（非同期）
         ├─→ 現在の不良データをローカルSQLiteに保存（非同期）
         │
         └─→ 共有SQLiteデータベースにマージ
               (SqlOperations.merge_target_database)

アプリケーション終了時 (__before_close)
         │
         ├─→ 残っている不良データをKintone APIに送信（非同期）
         ├─→ 残っている不良データをローカルSQLiteに保存（非同期）
         │
         └─→ 共有SQLiteデータベースにマージ
               (SqlOperations.merge_target_database)
               ├─ 新規・更新データのマージ
               └─ 削除IDリストの反映
```

### 4. データ保存先の詳細

#### 4.1 ローカルSQLiteデータベース

- **ファイル名**: `aoi_data.db`
- **保存場所**: プロジェクトディレクトリ（実行ファイルと同じディレクトリ）
- **用途**: 
  - 高速なローカルアクセス
  - オフライン動作時のデータ保存
  - 共有データベースとの差分管理

**保存タイミング:**
- 不良情報保存時 (`save_defect_info`)
- 不良情報削除時 (`delete_defect_info`)
- 指図変更時 (`change_lot`)
- アプリケーション終了時 (`__before_close`)

#### 4.2 共有SQLiteデータベース

- **ファイル名**: `aoi_data.db`
- **保存場所**: 共有ネットワークディレクトリ (`shared_directory`)
- **用途**:
  - 複数クライアント間でのデータ共有
  - 中央集約的なデータ管理

**同期タイミング:**
- アプリケーション起動時（共有DB → ローカルDB へコピー）
- 指図変更時（ローカルDB → 共有DB へマージ）
- アプリケーション終了時（ローカルDB → 共有DB へマージ）

**マージ処理:**
```python
SqlOperations.merge_target_database(
    self.sqlite_db_dir,      # ローカルDBディレクトリ
    self.shared_directory,    # 共有DBディレクトリ
    self.db_name,             # データベースファイル名
    delete_defect_ids=self.delete_defect_ids  # 削除IDリスト
)
```

#### 4.3 Kintone API

- **用途**: クラウドベースのデータ管理・レポート機能
- **送信タイミング**:
  - 不良情報保存時（非同期）
  - 不良情報削除時（非同期）
  - 指図変更時（非同期）
  - アプリケーション終了時（非同期）

**非同期処理:**
- Tkinterのメインスレッドをブロックしないよう、別スレッドで実行
- エラー時はステータスバーに表示（ユーザーへの通知）

#### 4.4 CSVファイル

- **保存場所**: `data_directory/{lot_number}/`
- **ファイル名**: `{lot_number}_{image_filename}.csv`
- **用途**:
  - レガシーシステムとの互換性
  - データのバックアップ
  - Excel等での手動編集

**保存タイミング:**
- 主に指図変更時（`defect_list_to_csv_async`）
- 非同期処理でファイルI/Oのブロッキングを回避

#### 4.5 画像ファイル

- **保存場所**: `data_directory/{lot_number}/`
- **ファイル名**: `{lot_number}_{board_index}_{defect_number}.png`
- **用途**:
  - 不良箇所の視覚的な記録
  - 座標マーカー付き画像の保存

**生成タイミング:**
- 不良情報保存時 (`save_defect_info`)
- 不良箇所に座標マーカーを描画して保存

---

## データ保存先の構成

### 現在の構成図

```
┌─────────────────────────────────────────────────────────┐
│                    AOI Defect History                    │
│                  (デスクトップアプリケーション)            │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ ローカルSQLite│  │  共有SQLite   │  │  Kintone API │
│  (aoi_data.db)│  │  (ネットワーク) │  │   (クラウド)  │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        │                 │                 │
        └─────────────────┴─────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │  CSVファイル      │
                │  (data_directory) │
                └──────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │  画像ファイル      │
                │  (data_directory) │
                └──────────────────┘
```

### データ保存先の特性比較

| 保存先 | データ形式 | アクセス速度 | 可用性 | 同期方法 | 主な用途 |
|--------|----------|------------|--------|---------|---------|
| ローカルSQLite | データベース | ⚡ 高速 | 常時利用可能 | 即座 | ローカル操作・キャッシュ |
| 共有SQLite | データベース | 🐌 中速（ネットワーク依存） | ネットワーク接続時のみ | マージ処理 | 複数クライアント間共有 |
| Kintone API | クラウドAPI | 🐌 中速（ネットワーク依存） | ネットワーク接続時のみ | 非同期送信 | レポート・分析 |
| CSVファイル | テキスト | ⚡ 高速 | 常時利用可能 | 手動/非同期 | バックアップ・互換性 |
| 画像ファイル | バイナリ | ⚡ 高速 | 常時利用可能 | 即座 | 視覚的記録 |

---

## データ同期とマージ処理

### マージ処理の仕組み

**問題点:**
- 複数のクライアントが同時に操作する場合、データの競合が発生する可能性
- マージ処理は指図変更時とアプリケーション終了時のみ実行
- リアルタイム同期ではない

**現在のマージ処理フロー:**

```
ローカルDB
    │
    ├─→ 共有DBにマージ
    │      ├─ 新規データの挿入 (merge_insert_defect_infos)
    │      ├─ 更新データのマージ (merge_insert_defect_infos)
    │      └─ 削除IDの反映 (delete_defect_ids)
    │
    └─→ マージ後の状態が共有DBに反映
```

**マージ処理のタイミング:**
1. **指図変更時**: 現在の指図のデータを保存してから次の指図へ
2. **アプリケーション終了時**: 残っているデータを保存してから終了

**懸念点:**
- 長時間動作しているクライアントのデータは、指図変更や終了まで共有DBに反映されない
- 複数クライアントが同じ指図を操作する場合、最後にマージしたクライアントのデータで上書きされる可能性

---

## 懸念点

### 1. データ整合性の問題

#### 1.1 競合状態（Race Condition）

**問題:**
- 複数のクライアントが同じ指図を同時に操作する場合、データの競合が発生
- マージ処理が指図変更時と終了時のみのため、リアルタイム同期がない
- 最後にマージしたクライアントのデータで上書きされる可能性

**例:**
```
クライアントA: 不良1を追加 → ローカルDBに保存
クライアントB: 不良2を追加 → ローカルDBに保存
クライアントA: 指図変更 → 共有DBにマージ（不良1のみ）
クライアントB: 指図変更 → 共有DBにマージ（不良2のみ）
結果: 不良1が失われる可能性
```

#### 1.2 データ損失のリスク

**問題:**
- アプリケーション異常終了時に、メモリ上のデータ（`defect_list`）が失われる
- ローカルDBへの保存は非同期のため、保存完了前に終了するとデータが失われる
- 共有DBへのマージが実行されないと、他のクライアントから見えない

#### 1.3 トランザクション管理の不足

**問題:**
- 複数の保存先（SQLite、Kintone、CSV）への保存が個別に実行される
- 一貫性保証がない（一部が成功、一部が失敗した場合の処理が不明確）
- ロールバック機能がない

### 2. パフォーマンスの問題

#### 2.1 ネットワーク依存のボトルネック

**問題:**
- 共有SQLiteデータベースへのアクセスがネットワーク経由のため、遅延が発生
- 大量のデータをマージする場合、処理時間が長くなる可能性
- ネットワーク障害時は共有DBにアクセスできない

#### 2.2 非効率なデータコピー

**問題:**
- アプリケーション起動時に共有DBをローカルにコピー（`shutil.copy`）
- 大きなデータベースの場合、起動時間が長くなる
- 差分更新ではなく、全体コピーを行うため非効率

#### 2.3 重複保存

**問題:**
- 同一データが複数の保存先（SQLite、CSV）に保存される
- ストレージ容量の無駄
- データ更新時の一貫性維持が困難

### 3. 可用性の問題

#### 3.1 単一障害点（SPOF: Single Point of Failure）

**問題:**
- 共有SQLiteデータベースがネットワーク上にあるため、ネットワーク障害時に全クライアントが影響を受ける可能性
- 共有ディレクトリへのアクセス権限の問題で、一部クライアントがデータにアクセスできない可能性

#### 3.2 オフライン動作時の制約

**問題:**
- ネットワーク接続が必須な部分がある（共有DB、Kintone API）
- オフライン時はローカルDBのみ使用可能だが、他のクライアントとの同期ができない

### 4. データベース選定の問題

#### 4.1 SQLiteの制約

**問題:**
- **同時書き込み制約**: SQLiteは同時書き込みに弱い
  - 複数クライアントが同時に書き込む場合、ロック競合が発生
  - ファイルベースのため、ネットワークファイルシステム上ではパフォーマンスが低下

- **スケーラビリティ**: 
  - データ量が増加すると、パフォーマンスが低下
  - 大量の同時接続には不向き

- **分散システムへの不適合**:
  - SQLiteは単一ファイルベースのため、分散システムには不適切
  - 複数クライアント間の同期には手動マージ処理が必要

#### 4.2 CSVファイルとの併用

**問題:**
- CSVとSQLiteの二重管理により、データ整合性が保証されない
- 更新時の同期処理が複雑
- データ形式が異なるため、変換処理が必要

---

## 改善点

### 1. データ整合性の改善

#### 1.1 トランザクション管理の導入

**推奨:**
- 複数の保存先への保存をトランザクションとして扱う
- すべての保存先への保存が成功した場合のみコミット
- 失敗時はロールバック（補償トランザクション）

**実装例:**
```python
def save_defect_info_with_transaction(self, defect_info):
    """トランザクション管理付きで不良情報を保存"""
    transaction_id = generate_transaction_id()
    
    try:
        # 1. ローカルSQLiteに保存
        self.sqlite_db.insert(defect_info)
        
        # 2. Kintone APIに送信
        kintone_result = self.kintone_client.post(defect_info)
        
        # 3. CSVファイルに保存
        save_to_csv(defect_info)
        
        # 4. 画像ファイルを保存
        save_image(defect_info)
        
        # すべて成功した場合のみコミット
        self.sqlite_db.commit()
        return True
        
    except Exception as e:
        # エラー時はロールバック
        self.sqlite_db.rollback()
        logger.error(f"トランザクション失敗: {e}")
        return False
```

#### 1.2 楽観的ロックの導入

**推奨:**
- データにバージョン番号やタイムスタンプを追加
- 更新時にバージョンをチェックし、競合を検出
- 競合時はユーザーに通知し、解決方法を提示

#### 1.3 イベント駆動型の同期

**推奨:**
- データ保存時に即座に共有DBに反映（非同期）
- 定期的なバッチ処理ではなく、リアルタイム同期
- 変更通知メカニズムの導入

### 2. パフォーマンスの改善

#### 2.1 差分同期の導入

**推奨:**
- アプリケーション起動時に共有DB全体をコピーするのではなく、差分のみ取得
- タイムスタンプや変更履歴を利用した差分検出
- 初回のみフルコピー、以降は差分のみ

#### 2.2 キャッシュ戦略の改善

**推奨:**
- よく使用されるデータをメモリキャッシュに保持
- LRU (Least Recently Used) キャッシュの導入
- 画像ファイルのキャッシュ機能

#### 2.3 バッチ処理の最適化

**推奨:**
- 複数の不良情報を一度に保存するバッチ処理
- ネットワークリクエストの削減
- トランザクションの効率化

### 3. 可用性の改善

#### 3.1 オフライン対応の強化

**推奨:**
- オフライン時はローカルDBのみ使用
- オンライン復帰時に自動的に同期
- 競合解決の仕組みを提供

#### 3.2 レプリケーション戦略

**推奨:**
- 複数の共有DBレプリカを用意
- 障害時の自動フェイルオーバー
- データの冗長化

### 4. データベース選定の改善

#### 4.1 集中型データベースへの移行検討

**推奨:**
- PostgreSQL や MySQL などのサーバーベースデータベースの導入
- 複数クライアント間の同時アクセスに対応
- ACID特性の保証

#### 4.2 CSVファイルの段階的廃止

**推奨:**
- CSVファイルとの併用を段階的に廃止
- データの一元管理（SQLiteまたはクラウドDBのみ）
- エクスポート機能としてCSVを提供

---

## 最適化された構成の提案

### 提案1: 集中型データベースアーキテクチャ

#### 構成図

```
┌─────────────────────────────────────────────────────────┐
│              PostgreSQL / MySQL サーバー                  │
│           (集中型データベース・中央集約管理)                │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ クライアントA  │  │ クライアントB  │  │ クライアントC  │
│ (ローカルDB)  │  │ (ローカルDB)  │  │ (ローカルDB)  │
│  キャッシュ用  │  │  キャッシュ用  │  │  キャッシュ用  │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┴─────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │   Kintone API     │
                │   (レポート用)     │
                └──────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │   画像ファイル     │
                │  (ファイルサーバー) │
                └──────────────────┘
```

#### メリット

1. **データ整合性の保証**
   - ACID特性による整合性保証
   - 同時書き込みの適切な処理
   - トランザクション管理

2. **パフォーマンスの向上**
   - ネットワーク経由でも高速アクセス可能
   - クエリ最適化機能
   - インデックスの効率的な使用

3. **スケーラビリティ**
   - 大量のデータに対応
   - 複数クライアント間の同時接続
   - 水平スケーリングが可能

4. **可用性の向上**
   - レプリケーション機能
   - 自動フェイルオーバー
   - バックアップ・リカバリ機能

#### デメリット

1. **インフラストラクチャの必要性**
   - データベースサーバーの構築・維持が必要
   - ネットワーク接続が必須

2. **運用コストの増加**
   - サーバーの運用・保守が必要
   - セキュリティ対策の強化が必要

### 提案2: ハイブリッド構成（現状を活かした改善）

#### 構成図

```
┌─────────────────────────────────────────────────────────┐
│                    AOI Defect History                    │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ ローカルSQLite│  │  共有SQLite   │  │  Kintone API │
│  (キャッシュ)  │◄─┤  (マスターDB) │  │   (レポート)  │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        │                 │                 │
        └─────────────────┴─────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │   画像ファイル     │
                │  (ファイルサーバー) │
                └──────────────────┘
```

#### 改善点

1. **役割の明確化**
   - **ローカルSQLite**: 高速キャッシュ・オフライン対応
   - **共有SQLite**: マスターデータベース（読み取り専用推奨）
   - **Kintone API**: レポート・分析専用

2. **リアルタイム同期の導入**
   - データ保存時に即座に共有DBへ同期（非同期）
   - ファイル監視（Watchdog）による変更検知
   - WebSocket等によるリアルタイム通知

3. **読み取り最適化**
   - 共有DBを読み取り専用にする
   - 書き込みはローカルDB経由で、非同期に共有DBへ反映
   - 読み取り時はローカルDBを優先（キャッシュ）

4. **CSVファイルの役割変更**
   - データ保存先から除外
   - エクスポート機能としてのみ提供
   - レガシーシステムとの連携時のみ使用

### 提案3: クラウドファースト構成

#### 構成図

```
┌─────────────────────────────────────────────────────────┐
│              Kintone / カスタムAPIサーバー                │
│           (クラウドベース・一元管理)                       │
└─────────────────────────────────────────────────────────┘
                          ▲
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ クライアントA  │  │ クライアントB  │  │ クライアントC  │
│              │  │              │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        └─────────────────┴─────────────────┘
                          │
                          ▼
                ┌──────────────────┐
                │   画像ファイル     │
                │  (クラウドストレージ)│
                └──────────────────┘
```

#### メリット

1. **シンプルな構成**
   - データ保存先がクラウドのみ
   - 同期処理が不要
   - メンテナンスが容易

2. **高い可用性**
   - クラウドサービスの冗長化
   - 自動バックアップ
   - 24/7稼働

3. **スケーラビリティ**
   - クラウドの自動スケーリング
   - ストレージの柔軟な拡張

#### デメリット

1. **ネットワーク依存**
   - オフライン動作が困難
   - ネットワーク遅延の影響

2. **コスト**
   - クラウドサービスの利用料金
   - データ転送量に応じた課金

### 推奨構成: 提案2（ハイブリッド構成）の段階的改善

**理由:**
- 既存のインフラストラクチャを最大限活用
- 段階的な移行が可能
- コスト効率が良い

#### 段階的改善プラン

**フェーズ1: 即座に実装可能な改善**
1. データ保存時の即座同期（非同期）
2. CSVファイルの段階的廃止
3. エラーハンドリングの強化

**フェーズ2: 中期改善（3-6ヶ月）**
1. 共有DBへの読み取り専用アクセスの導入
2. 差分同期の実装
3. トランザクション管理の導入

**フェーズ3: 長期改善（6-12ヶ月）**
1. PostgreSQL/MySQLへの移行検討
2. リアルタイム同期の導入
3. オフライン機能の強化

---

## まとめ

現在のAOI Defect Historyのデータフローは、複数のデータ保存先を使用した分散型システムです。機能としては動作していますが、以下の点で改善の余地があります：

1. **データ整合性**: 複数クライアント間の競合処理
2. **パフォーマンス**: ネットワーク経由の同期処理
3. **可用性**: オフライン動作時の制約
4. **データベース選定**: SQLiteの制約（分散システムへの不適合）

推奨される改善は、**ハイブリッド構成の段階的改善**です。既存のインフラストラクチャを活かしながら、段階的に最適化していくことが現実的です。

将来的には、**集中型データベース（PostgreSQL/MySQL）への移行**も検討する価値があります。特に、データ量の増加やクライアント数の増加が見込まれる場合には、早めの移行が推奨されます。

