# クラウドファースト構成への移行ガイド

## 移行日
2024年12月

## 概要

AOI Defect Historyアプリケーションを、Kintone APIを主要データ保存先とするクラウドファースト構成に移行しました。

## 変更内容

### 1. データ保存先の変更

#### 変更前（分散型構成）
- **主要データ保存先**: 共有SQLiteデータベース（ネットワーク共有ディレクトリ）
- **補助データ保存先**: ローカルSQLite、Kintone API、CSVファイル
- **同期方法**: マージ処理による手動同期

#### 変更後（クラウドファースト構成）
- **主要データ保存先**: **Kintone API**（クラウド）
- **キャッシュ/オフライン対応**: ローカルSQLite（オフライン時のフォールバック）
- **エクスポート機能**: CSVファイル（手動エクスポートのみ）
- **同期方法**: リアルタイム同期（Kintone API経由）

### 2. 削除された機能

#### 共有SQLiteデータベース
- **削除**: 共有SQLiteデータベースへのマージ処理
- **削除**: アプリケーション起動時の共有DBコピー処理
- **削除**: 指図変更時の共有DBマージ処理
- **削除**: アプリケーション終了時の共有DBマージ処理

#### CSVファイルの自動保存
- **削除**: 指図変更時の自動CSV保存
- **変更**: CSVエクスポート機能として手動エクスポートのみ提供

### 3. 追加された機能

#### CSVエクスポート機能
- **追加**: メニュー「ファイル」→「CSVにエクスポート」
- **用途**: レガシーシステムとの連携、バックアップ、手動編集

#### Kintone APIからのデータ読み込み
- **追加**: `__read_defect_list_from_kintone()` メソッド
- **動作**: 指図変更時にKintone APIから優先的にデータを取得
- **フォールバック**: Kintone APIが利用できない場合はローカルDBから読み込み

### 4. 変更されたデータフロー

#### データ保存フロー（変更後）

```
ユーザー入力
    │
    ▼
メモリ上（defect_list）
    │
    ├─→ Kintone API（優先・非同期）
    │      └─ 主要なデータ保存先
    │
    ├─→ ローカルSQLite（非同期）
    │      └─ キャッシュ/オフライン対応
    │
    └─→ 画像ファイル（即座）
          └─ 視覚的記録
```

#### データ読み込みフロー（変更後）

```
指図変更時
    │
    ├─→ Kintone APIから読み込み（優先）
    │      ├─ 成功: データを表示
    │      └─ 失敗: ローカルDBから読み込み（フォールバック）
    │
    └─→ ローカルDBにキャッシュとして保存
```

### 5. 設定ファイルの変更

#### shared_directoryの非推奨化
- **変更**: `shared_directory`設定は非推奨として扱われる
- **後方互換性**: 既存の設定ファイルは読み込まれるが、使用されない
- **警告表示**: `shared_directory`が設定されている場合、警告メッセージを表示

#### 設定ウィンドウの変更
- **変更**: 共有ディレクトリ設定欄を無効化（グレーアウト）
- **表示**: 「(非推奨 - クラウドファースト構成ではKintone APIを使用)」と表示

## 移行手順

### ステップ1: 環境変数の設定

`.env`ファイルにKintone APIの認証情報を設定してください：

```env
# Kintone API設定（AOI検査用）
KINTONE_SUBDOMAIN=your_subdomain_here
KINTONE_APP_ID=your_app_id_here
KINTONE_API_TOKEN=your_api_token_here
```

### ステップ2: 既存データの移行

既存の共有SQLiteデータベースのデータをKintone APIに移行する必要があります。

**移行方法:**
1. 既存の共有SQLiteデータベースからデータをエクスポート
2. Kintone APIにデータをインポート
3. データの整合性を確認

### ステップ3: 設定ファイルの更新

`settings.ini`ファイルの`shared_directory`設定は削除または空にしてください：

```ini
[DIRECTORIES]
image_directory = //path/to/image
data_directory = //path/to/data
schedule_directory = //path/to/schedule
shared_directory =  # 空にするか削除
```

### ステップ4: アプリケーションの再起動

変更を反映するため、アプリケーションを再起動してください。

## メリット

### 1. データ整合性の向上
- **リアルタイム同期**: Kintone API経由で即座に同期
- **競合の解消**: 複数クライアント間でのデータ競合が発生しない
- **一元管理**: すべてのデータがKintone APIで一元管理される

### 2. 可用性の向上
- **24/7稼働**: クラウドサービスのため常時利用可能
- **自動バックアップ**: Kintoneの自動バックアップ機能を利用
- **障害復旧**: クラウドサービスの冗長化により高い可用性

### 3. 運用の簡素化
- **マージ処理の不要**: 手動マージ処理が不要
- **設定の簡素化**: 共有ディレクトリの設定が不要
- **メンテナンスの削減**: データベースサーバーのメンテナンスが不要

### 4. スケーラビリティ
- **データ量の増加に対応**: クラウドサービスのスケーラビリティを活用
- **クライアント数の増加に対応**: 複数クライアント間での同時アクセスに対応

## 注意事項

### 1. ネットワーク接続の必要性

**重要**: Kintone APIを使用するため、インターネット接続が必要です。

- **オフライン時**: ローカルSQLiteキャッシュから読み込み可能
- **オフライン時の保存**: ローカルSQLiteに保存され、オンライン復帰時に自動的にKintone APIに送信

### 2. Kintone APIの制限

Kintone APIには以下の制限があります：

- **API呼び出し回数**: 1時間あたりのAPI呼び出し回数に制限がある場合があります
- **データサイズ**: 1回のAPI呼び出しで送信できるデータサイズに制限があります
- **レート制限**: 短時間での大量アクセスは制限される可能性があります

### 3. データ移行の必要性

既存の共有SQLiteデータベースのデータをKintone APIに移行する必要があります。

**推奨手順:**
1. 既存データのバックアップを取得
2. データ移行スクリプトの実行
3. データの整合性確認
4. 移行完了後の動作確認

### 4. 後方互換性

- **shared_directory設定**: 既存の設定ファイルは読み込まれますが、使用されません
- **ローカルSQLite**: 既存のローカルSQLiteデータベースはキャッシュとして使用されます
- **CSVファイル**: 既存のCSVファイルは読み込み可能ですが、自動保存は行われません

## トラブルシューティング

### Kintone APIに接続できない

**原因:**
- 環境変数が正しく設定されていない
- ネットワーク接続の問題
- Kintone APIの認証情報が間違っている

**対処法:**
1. `.env`ファイルの設定を確認
2. ネットワーク接続を確認
3. Kintone APIの認証情報を確認

### データが表示されない

**原因:**
- Kintone APIからデータを取得できない
- ローカルDBにキャッシュがない

**対処法:**
1. Kintone APIの接続状態を確認
2. 指図番号が正しいか確認
3. ローカルDBのキャッシュを確認

### オフライン時の動作

**動作:**
- ローカルSQLiteキャッシュからデータを読み込み
- 新しいデータはローカルSQLiteに保存
- オンライン復帰時に自動的にKintone APIに送信

## 今後の改善予定

1. **Kintone APIからのデータ読み込み機能の強化**
   - フィルタリング機能の追加
   - ページネーション対応

2. **オフライン機能の強化**
   - オフライン時の変更履歴管理
   - オンライン復帰時の自動同期

3. **エラーハンドリングの改善**
   - リトライ機能の強化
   - エラー通知の改善

4. **パフォーマンスの最適化**
   - バッチ処理の導入
   - キャッシュ戦略の改善

## まとめ

クラウドファースト構成への移行により、以下の改善が実現されました：

- ✅ **データ整合性の向上**: リアルタイム同期による競合の解消
- ✅ **可用性の向上**: 24/7稼働と自動バックアップ
- ✅ **運用の簡素化**: マージ処理の不要化
- ✅ **スケーラビリティ**: データ量・クライアント数の増加に対応

Kintone APIを主要データ保存先として使用することで、より堅牢で保守しやすいシステムになりました。

